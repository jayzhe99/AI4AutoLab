[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ai4autolab"
version = "0.1.0"
description = "An LLM-tool framework for flexible, skill-driven autonomous laboratories."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
